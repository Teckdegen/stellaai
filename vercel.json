{
  "buildCommand": "node scripts/install-clarinet.cjs && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "routes": [
    {
      "src": "/api/validate",
      "dest": "/app/api/validate/route.ts"
    }
  ]
}
